# Frontier Lang JSX Adapter Notes

Keep this package focused on runtime-neutral JSX source evidence: element identity, prop records, keyed children, child structure, and fail-closed proof gaps for runtime behavior.

Project-level JS/TS/TSX merge admission belongs in `@shapeshift-labs/frontier-lang-compiler`. Type-checker-backed TSX evidence belongs in `@shapeshift-labs/frontier-lang-typescript` or the compiler facade.

Do not print or commit npm tokens. Release credentials live in `/Users/james/Documents/json-diff/.env`.
